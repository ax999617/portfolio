source_root: "<local-source>/ml-training"
task: "3-class image recognition"
num_classes: 3
model_architecture: "resnet18"
preprocessing:
  image_size: [224, 224]
  normalization_mean: [0.485, 0.456, 0.406]
  normalization_std: [0.229, 0.224, 0.225]
extracted_from:
  dataset_processing: "<local-source>/ml-training/dataset_processing.py"
  trained_model: "<local-source>/ml-training/trained.py"
  quantization_pipeline: "<local-source>/ml-training/quantized.py"
runnable_defaults:
  batch_size: 16
  epochs: 10
  learning_rate: 0.001
notes: "Defaults are provided for reproducible entrypoints; no new training was run during portfolio consolidation."
